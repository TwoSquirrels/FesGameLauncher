/*!
license header
*/

// references

/// <reference path="script.ts"/>

// define functions

// register events

window.onload = event => {
  maximizingAdjustment();
};

window.onresize = event => {
  maximizingAdjustment();
};
